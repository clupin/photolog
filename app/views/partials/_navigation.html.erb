<header>
	<div class="row be-green txt-white large-padding">
		<div class="col-md-9">
			<%= link_to "Inicio", root_path %>
		</div>
		<div class="col-md-3">
			<% if user_signed_in? %>
				<%= link_to "Cerrar Sesión", destroy_user_session_path, method: :delete %>
			<% else %>
				<%= link_to "Ingresar", new_user_session_path %>
			<% end %>
		</div>
	</div>
</header>